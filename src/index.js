import * as React from "react";
import * as ReactDOM from "react-dom";

const root = document.querySelector("#app");

ReactDOM.render(
  <div>
    <h1>hello</h1>
  </div>,
  root
);

export default "This shoudld be visible with window.starter.default"

